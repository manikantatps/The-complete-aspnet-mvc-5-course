@* View Date Example *@
@*@using Vidly.Models
    @Model Example
    @model Vidly.Models.Movie*@
@* ViewModel Example *@
@model Vidly.ViewModels.RandomMovieViewModel
@{
    ViewBag.Title = "Random";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<h2>@Model.Name</h2>
    <h2>Manikanta</h2>*@

@*<h2>View Data Sample</h2>
    <h1>@(((Movie)ViewData["Movie"]).Name)</h1>*@

<h2>View Model</h2>
<h2>@Model.Movie.Name</h2>

@if (Model.Customers.Count == 0)
{
    //the below text tag is not HTML. its a razor
    <text>No one has rented this movie before</text>
}
else
{
    <ul>
        @foreach (var customer in Model.Customers)
        {
            <li>@customer.Name</li>
        }
    </ul>
}

@* Other Razor view example *@
@* The below example shows how to bind the class name to class *@
@{
    var className = Model.Customers.Count > 10 ? "popular" : null;
}

<h2 class="@className">@Model.Movie.Name</h2>